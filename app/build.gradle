plugins {
    id 'com.android.application'
}

android {
    namespace 'com.consolekey.android'
    compileSdk 35

    defaultConfig {
        applicationId 'com.consolekey.android'
        minSdk 26
        targetSdk 35
        versionCode 7
        versionName '0.1.6'
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
